# Optional. Override the default Hugging Face Hub repo.
# HF_REPO_ID=ethsmaa/cinesentiment-distilbert-imdb

# Optional. Only required if HF_REPO_ID is private.
# Get a token from https://huggingface.co/settings/tokens (read scope is enough).
# HF_TOKEN=hf_xxxxxxxxxxxxxxxxxxxxxxxx

# Phase 3 (FastAPI) will add: PORT, MODEL_DIR, etc.
